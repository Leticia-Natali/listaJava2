function diferencaNumeros() {
    let a= 0;
    let b= 0;

    while (continuar) {
        let numero= parseFloat(prompt("Digite um número para saber a diferença:")); 
    diferenca= a - b;
}

alert("A diferença total dos números foi de:", + diferenca);

}